---
description: Genera y administra un simulacro de una evaluación registrada dentro de una unidad.
---

Ejecutá la acción `simulacro` usando la skill `simulacro` disponible en este proyecto. Cargá la skill con la herramienta `skill` antes de actuar y seguí su `SKILL.md` exactamente, incluyendo el router, la resolución de argumentos y el pipeline que indique. No reimplementes ni sustituyas el flujo.

Argumentos del usuario: $ARGUMENTS
